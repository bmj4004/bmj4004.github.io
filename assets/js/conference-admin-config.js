/*
 * Public configuration for the conference editor.
 * Set apiBase to the deployed Worker URL, without a trailing slash.
 * This file must never contain a GitHub token, OAuth secret, or password.
 */
window.CONFERENCE_ADMIN_CONFIG = Object.freeze({
  apiBase: '',
});
